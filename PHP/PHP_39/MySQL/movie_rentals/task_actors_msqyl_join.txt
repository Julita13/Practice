1. Į `movie_rentals`.`movies` lentelę pridėti stulpelį `actor_id`:

    `actor_id` - skaičius, ne minusinis, gali būti NULL;

2. Atnaujinti įrašus, kad kiekvienas `movies` įrašas turėtų `actor_id`, 
kuris atitinka lentelėje `actors` esenčio įrašo id. Vieną `actor_id` palikti null.

3. Parašyti SELECT `movies` užklausą, kuria paimami įrašai su aktoriaus vardu ir pavarde.
Rezultate turi matytis. 

filmo pavadinimas,
filmo data
aktoriaus vardas pavardė
aktoriaus gimimo data

4. Parašyti FK constraints `movies` lentelei stulpeliams `actor_id` ir `genre_id`. 

